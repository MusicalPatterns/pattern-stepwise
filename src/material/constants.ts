// tslint:disable no-magic-numbers

import { as, Scalar } from '@musical-patterns/utilities'

const REDUCE_GAIN_BECAUSE_SAMPLES_ARE_REALLY_LOUD: Scalar<Scalar> = as.Scalar<Scalar>(0.25)

export {
    REDUCE_GAIN_BECAUSE_SAMPLES_ARE_REALLY_LOUD,
}
