// tslint:disable no-magic-numbers

import { Landing, stepwiseAs } from '../nominals'

const THREE_PER_PIECE_LANDING_RANGE: Landing = stepwiseAs.Landing(9)

export {
    THREE_PER_PIECE_LANDING_RANGE,
}
